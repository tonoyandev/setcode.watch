<script setup lang="ts">
useHead({ title: 'Subscribe — SetCode.watch' });
</script>

<template>
  <div class="stub">
    <h1>Subscribe to alerts</h1>
    <p>
      Full confirmation flow lands in a coming step. The pipeline is already wired end-to-end on
      the backend — see the bot for a direct Telegram entrypoint.
    </p>
  </div>
</template>

<style scoped>
.stub {
  max-width: var(--width-content);
  margin: var(--space-16) auto;
  padding: 0 var(--space-6);
}
.stub p {
  color: var(--color-ink-muted);
  margin-top: var(--space-4);
  font-size: var(--text-lg);
}
</style>
