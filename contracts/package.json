{
  "name": "@setcode/contracts",
  "version": "0.0.0",
  "private": true,
  "description": "SetCode.watch Solidity contracts (Foundry)",
  "license": "MIT",
  "scripts": {
    "build": "forge build",
    "test": "forge test",
    "test:ci": "FOUNDRY_PROFILE=ci forge test -vvv",
    "snapshot": "forge snapshot",
    "snapshot:check": "forge snapshot --check --tolerance 2",
    "fmt": "forge fmt",
    "fmt:check": "forge fmt --check",
    "typecheck": "forge build --sizes"
  }
}
